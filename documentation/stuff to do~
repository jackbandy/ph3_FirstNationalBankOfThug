DONE:
Added Ids:
    creation
    eq
    poly
    state
    dim_*
    mesh_*
    reyn
    pos_*
    flow_*
    func_*
    solve
    clear
    solution
    plot_type
    plot
    refine_type
    refine
    m_refine
    load
    load_file
    save
    save_file
    plot
    error
Regular expression to parse position stuff
    Use the string "([x,y][=,>,<]/d+)+" Maybe <=, >=, =<, >=  from stackoverflow r"Test(\d*(?:\.\d+))"
Fix interpreter2.py
if Navier Stokes its always steady state
Depending on the state of inflow and outflow grey out

STUFF TO DO:
pass -1 in the reyNum if stokes
Bridge the stuff
    manualRefine(type, numbers) gives energy error
    autoRefine(type) gives energy error
    solve(lots of stuff [inflow tuples of strings], [outflow list of strings]) gives energy error string
    plot(type) returns image?? options u1, u2, p, stream, mesh, error
    save(string) *
    load(string) * (maybe return all the stuff)

call plot if we refine
PLOT AND ENERGY ERROR!!! AAAAAAAAHHHHHHHH!!!!!

